## 2024-06-29 - Fixed CSRF Vulnerability in OAuth Consent Form

**Vulnerability:** A CSRF (Cross-Site Request Forgery) vulnerability allowed any website to trigger the OAuth authorization consent form via a direct POST request without the user's authorization or intent, potentially leaking the authorization code or granting access.
**Learning:** The absence of CSRF protection token validation, particularly using standard stateful or stateless paradigms like Double Submit Cookie, in endpoints that serve state-changing POST forms can lead to unintended approvals of authorization grants.
**Prevention:** Ensured the consent GET form correctly embeds a stateless cryptographic CSRF token, and required its presence and validation against a secure `Lax` or `Strict` cookie upon the subsequent POST request. Also, set the `X-Frame-Options: DENY` header on the consent form page to prevent Clickjacking.
## 2024-06-30 - Fix DoS via UnicodeDecodeError in Middleware
**Vulnerability:** A Denial of Service (DoS) vulnerability was found in `OAuthMiddleware`. Calling `headers.get(b"authorization", b"").decode()` blindly crashes the server (500 Error) if a user supplies invalid UTF-8 bytes in the `Authorization` header.
**Learning:** Starlette/ASGI apps deliver headers as raw bytes. Blindly decoding them to UTF-8 without a try/except block introduces unhandled exceptions that attackers can easily trigger to bypass logic or crash endpoints.
**Prevention:** Always read headers as raw bytes. Validate known prefixes (like `b"Bearer "`) using byte strings. Wrap any `.decode()` calls in a try-except block handling `UnicodeDecodeError`, failing gracefully (e.g., returning 401 instead of 500).
## 2024-07-01 - Fix ASGI Protocol Violation DoS in WebSocket Middleware
**Vulnerability:** A Denial of Service (DoS) vulnerability existed in `OAuthMiddleware`. When a client established an unauthenticated WebSocket connection (`scope["type"] == "websocket"`), the middleware attempted to reject it by calling `_send_json`, which dispatches HTTP response events (`http.response.start`). Sending HTTP events over an ASGI websocket scope causes the ASGI server (e.g., Uvicorn) to crash the connection with a `RuntimeError` due to a protocol violation, rather than failing securely.
**Learning:** Middleware handling both HTTP and WebSockets must respect ASGI protocol differences. Security rejections (like 401 Unauthorized) cannot be sent identically to both connection types. WebSockets require explicit `{"type": "websocket.close", "code": 1008}` events to indicate policy violations.
**Prevention:** Always check `scope["type"]` before sending rejection events in middleware. Ensure that WebSocket rejections dispatch `websocket.close` with an appropriate code (e.g., 1008 Policy Violation) instead of reusing HTTP-specific error handlers.
## 2024-07-02 - Add Cache-Control Headers to Prevent Credential Leakage
**Vulnerability:** The `/register` and `/token` endpoints previously returned JSON responses containing sensitive data (e.g., `client_secret` and `access_token`) without explicitly forbidding caching. This creates a risk where intermediary proxies or browser caches could store these secrets, leading to credential leakage.
**Learning:** Returning sensitive credentials in HTTP responses requires explicit headers (`Cache-Control: no-store` and `Pragma: no-cache`) to ensure they are not cached by any network intermediaries, as mandated by OAuth 2.0 (RFC 6749) and OAuth 2.0 Dynamic Client Registration (RFC 7591).
**Prevention:** Always ensure that endpoints returning access tokens, refresh tokens, client secrets, or other sensitive authentication materials include `headers={"Cache-Control": "no-store", "Pragma": "no-cache"}` in the HTTP response.
## 2024-07-06 - Prevent DoS via Invalid JSON Type in Register Endpoint
**Vulnerability:** A minor Denial of Service (DoS) vulnerability existed in the `/register` endpoint because `body = await request.json()` does not guarantee the parsed JSON is a dictionary. If a client sends a JSON array or string (e.g., `["foo"]`), the subsequent call to `body.get()` triggers an `AttributeError`, resulting in a 500 Internal Server Error crash.
**Learning:** `request.json()` can return lists, strings, ints, or booleans depending on the client payload. Applications must actively type-check the returned parsed JSON before interacting with it to ensure stability.
**Prevention:** Always use `isinstance(body, dict)` after parsing JSON to ensure it is the expected dictionary before performing `.get()` or dictionary lookups on the payload.
## 2025-02-14 - SSRF/CIMD Bypass in Authorization Endpoint
**Vulnerability:** The `/authorize` endpoint unconditionally fetched Client ID Metadata Documents (CIMD) for unknown HTTPS client IDs, even when `public_registration` was set to `False`. This bypassed the private registration mode and exposed the server to potential Server-Side Request Forgery (SSRF) and Denial of Service (DoS).
**Learning:** External or remote fetches based on user-supplied input must be strictly gated by application configuration state, not just input validation.
**Prevention:** Ensure feature flags (like `public_registration`) are validated *before* initiating any external network requests or dynamic registration workflows.
## 2025-02-20 - Fix DoS via ValueError in urlparse for Malicious URLs
**Vulnerability:** A Denial of Service (DoS) vulnerability existed in `origo/endpoints.py`. Unvalidated user-supplied inputs (`client_id` and `redirect_uris`) were passed directly to `urllib.parse.urlparse`. Malformed URLs (such as `https://]/`) cause `urlparse` to raise an uncaught `ValueError` (e.g., "Invalid IPv6 URL"). This unhandled exception caused the ASGI server to return a 500 Internal Server Error, allowing attackers to easily crash request handling on critical endpoints like `/authorize` and `/register`.
**Learning:** `urllib.parse.urlparse` is not completely safe to call on arbitrary string inputs in recent Python versions; it actively validates parts of the URL structure (like IPv6 brackets and port ranges) and can raise exceptions that must be handled by robust web applications.
**Prevention:** Always wrap `urlparse` calls dealing with untrusted user input in a `try...except ValueError` block to safely catch parsing errors and return appropriate validation failures (like 400 Bad Request) instead of crashing.
## 2025-02-21 - Fix DoS via ValueError in urlparse for Malicious URLs
**Vulnerability:** A Denial of Service (DoS) vulnerability existed in `origo/endpoints.py`. Unvalidated user-supplied inputs (`redirect_uri`) were passed directly to `urllib.parse.urlparse`. Malformed URLs (such as `https://]/`) cause `urlparse` to raise an uncaught `ValueError` (e.g., "Invalid IPv6 URL"). This unhandled exception caused the ASGI server to return a 500 Internal Server Error, allowing attackers to easily crash request handling on endpoints like `/authorize`.
**Learning:** `urllib.parse.urlparse` is not completely safe to call on arbitrary string inputs in recent Python versions; it actively validates parts of the URL structure (like IPv6 brackets and port ranges) and can raise exceptions that must be handled by robust web applications.
**Prevention:** Always wrap `urlparse` calls dealing with untrusted user input in a `try...except ValueError` block to safely catch parsing errors and return appropriate validation failures (like 400 Bad Request) instead of crashing.
## 2025-02-21 - Fix DoS and Prevent CRLF in _build_redirect
**Vulnerability:** A Denial of Service (DoS) vulnerability existed in `origo/endpoints.py` where `_build_redirect` called `urllib.parse.urlparse` on arbitrary URIs without a try/except block. Malformed URIs raise `ValueError`. Importantly, catching this and returning the raw string opens the door to CRLF injection, as `ValueError` is raised specifically to block URIs with control characters.
**Learning:** `ValueError` from `urlparse` isn't just a parse failure; it's a security mechanism preventing HTTP Response Splitting. It must be explicitly caught and treated as an invalid/malicious request rather than safely suppressed.
**Prevention:** Wrap `urlparse` calls dealing with potentially untrusted URI generation in a `try...except ValueError` block. Raise or handle the error securely (e.g., returning 400 Bad Request) instead of ignoring it or returning the unparsed, potentially dangerous string.
## 2025-02-21 - Fix SSRF via IPv4-mapped and Non-Global IPv6/IPv4 in _is_public_host
**Vulnerability:** The `_is_public_host` function in `origo/endpoints.py` aims to prevent Server-Side Request Forgery (SSRF) when fetching Client ID Metadata Documents by blocking resolutions to private, loopback, or link-local IPs. However, it used manual checks (`ip.is_private`, `ip.is_loopback`) that fail to block Carrier-Grade NAT (CGNAT, e.g., `100.64.0.0/10`) and other non-global IP spaces in Python 3. Furthermore, IPv4-mapped IPv6 addresses (e.g., `::ffff:192.168.1.1`) might bypass simplistic checks on some network configurations. This allowed attackers to perform SSRF against internal infrastructure residing on non-RFC1918 but still internal/private subnets.
**Learning:** `ipaddress.is_private` is not synonymous with "internal" or "non-routable" on the public internet, particularly for ranges like CGNAT or IETF protocol assignments. Reimplementing public routing logic manually using multiple boolean checks is prone to bypasses.
**Prevention:** Unpack IPv4-mapped IPv6 addresses manually if required, and leverage `not ip.is_global` (alongside existing checks for maximum defense-in-depth) as the definitive indicator of whether an IP is truly meant for the public internet.
## 2025-02-21 - Prevent DoS via Unhandled Exceptions in JSON Parsing
**Vulnerability:** A Denial of Service (DoS) vulnerability existed in `origo/endpoints.py` where `_fetch_client_metadata_document` parsed external JSON responses (`json.loads(body)`) but only caught `json.JSONDecodeError`. If the response contained invalid UTF-8 bytes, a `UnicodeDecodeError` was raised. Additionally, if the response was a valid JSON list (e.g., `["not a dict"]`), a subsequent call to `.get()` resulted in an `AttributeError`. These unhandled exceptions would crash the server (500 Error).
**Learning:** `json.loads` on raw bytes can raise `UnicodeDecodeError`, and it can return lists, strings, integers, or booleans. Relying on an expected dictionary structure without validation introduces crashes.
**Prevention:** Catch `UnicodeDecodeError` alongside `json.JSONDecodeError` when decoding untrusted byte sequences. Additionally, explicitly validate the parsed object type using `isinstance(data, dict)` before interacting with its contents.
## 2025-02-21 - Prevent DoS via UnicodeEncodeError in PKCE Verification
**Vulnerability:** A Denial of Service (DoS) vulnerability existed in `origo/endpoints.py` where `_verify_pkce` called `code_verifier.encode()` unconditionally. If a malicious client provided an unencodable string (such as one containing lone surrogate pairs like `\ud800`), it would trigger an unhandled `UnicodeEncodeError`. This unhandled exception crashed the request handler, resulting in a 500 Internal Server Error.
**Learning:** In Python, `.encode()` is not a completely safe operation on arbitrary untrusted strings. Invalid characters such as surrogate pairs will raise a `UnicodeEncodeError`, which robust applications must explicitly catch to prevent application crashes on invalid input.
**Prevention:** Always wrap `.encode()` calls on user-controlled input within a `try...except UnicodeEncodeError` block, and handle the failure securely (e.g., returning `False` for a validation function or `400 Bad Request` in an endpoint).
## 2025-02-21 - Prevent DoS via TypeError in compare_digest
**Vulnerability:** A Denial of Service (DoS) vulnerability existed in `origo/endpoints.py` where `hmac.compare_digest` and `secrets.compare_digest` were called directly with untrusted user inputs (such as `client_secret`, `csrf_token`, and `code_challenge`). In Python, these functions raise a `TypeError` if they are passed strings containing non-ASCII characters. A malicious client could supply a non-ASCII string (e.g., `"äbc"`), triggering an unhandled exception and crashing the request handler (500 Internal Server Error).
**Learning:** `hmac.compare_digest` and `secrets.compare_digest` expect ASCII-only strings or bytes. They are not entirely safe to run unconditionally against arbitrary untrusted text inputs. An unhandled `TypeError` from these functions can easily be weaponized for DoS.
**Prevention:** Wrap calls to `compare_digest` on potentially untrusted strings in a `try...except TypeError` block, and handle the failure securely by returning `False` (treating the comparison as failed) rather than crashing the endpoint.
## 2025-02-21 - Fix HTTP Request Smuggling / Auth Bypass in OAuthMiddleware
**Vulnerability:** A High severity authentication bypass vulnerability existed in `origo/middleware.py`. The middleware used `dict(scope.get("headers", []))` to extract the `Authorization` header. `dict()` inherently deduplicates keys by keeping the *last* value. However, downstream frameworks like Starlette extract the *first* value. An attacker could send two `Authorization` headers: `Authorization: Bearer ADMIN_TOKEN` and `Authorization: Bearer LOW_PRIV_VALID_TOKEN`. The middleware would validate the valid low-privilege token and allow the request, but the application would process the request using the forged admin token.
**Learning:** ASGI headers are a list of tuples precisely because HTTP allows multiple headers with the same name. Using `dict()` to parse them silently drops duplicates, creating critical inconsistencies between security middlewares and downstream applications (HTTP Header Smuggling).
**Prevention:** Always iterate through the raw ASGI header list to count and extract sensitive headers like `Authorization`. Explicitly reject requests (e.g., 400 Bad Request) that contain multiple instances of headers where only one is expected, per RFC 6750.
## 2025-02-21 - Fix HTTP Request Smuggling / Auth Bypass in Endpoints
**Vulnerability:** A High severity authentication bypass vulnerability existed in `origo/endpoints.py`. The `/token` and `/userinfo` endpoints used `request.headers.get("Authorization")` which silently drops duplicate headers and returns only the *first* one. An attacker could send two `Authorization` headers: `Authorization: Bearer ADMIN_TOKEN` and `Authorization: Bearer LOW_PRIV_VALID_TOKEN`. If intermediate proxies handle duplicates differently, they might validate one token while the application processes the other.
**Learning:** Starlette's `request.headers.get('Header-Name')` keeps only the first header, which can cause inconsistencies between security components and downstream applications (HTTP Header Smuggling).
**Prevention:** Always use Starlette's `request.headers.getlist('Header-Name')` to count and extract sensitive headers like `Authorization`. Explicitly reject requests (e.g., 400 Bad Request) that contain multiple instances of headers where only one is expected.
## 2025-02-21 - Fix DoS via Cache Eviction
**Vulnerability:** A Denial of Service (DoS) vulnerability existed in `origo/storage.py` where the dynamic client registration logic managed capacity by evicting the oldest client when the `max_dynamic_clients` limit was reached. An attacker could exploit this by continuously registering new clients, systematically evicting legitimate clients and invalidating their sessions.
**Learning:** Relying on silent cache eviction to manage capacity for unbounded resource allocations creates a DoS vulnerability. Attackers can spam allocations to arbitrarily evict legitimate clients.
**Prevention:** Explicitly enforce limits by rejecting new registrations (e.g., returning a 429 HTTP response, or raising a `ValueError`) when capacity is reached.
## 2025-02-21 - Ensure JWKS Endpoint is Publicly Accessible
**Vulnerability:** The JSON Web Key Set (JWKS) endpoint (`/.well-known/jwks.json`) was omitted from the `_PUBLIC_PATHS` set in `OAuthMiddleware`. Since JWKS is required by OIDC clients to fetch the public key for verifying signed ID tokens, protecting it behind a Bearer token requirement creates a chicken-and-egg problem, breaking the authentication flow and rendering ID token verification impossible.
**Learning:** Endpoints designed for discovery and public key distribution (like `jwks.json` and OpenID discovery endpoints) must be explicitly whitelisted from authentication middleware to ensure proper functioning of OAuth2/OIDC protocols.
**Prevention:** Always verify that all `.well-known` discovery and key distribution endpoints are included in the middleware's public path allowlist. Add explicit integration tests asserting that these specific paths return 200 OK without any `Authorization` headers.

## 2024-07-29 - Prevent DoS from unhandled urlparse ValueError
**Vulnerability:** When parsing a user-provided `client_id` (e.g. `client_is_https = urlparse(client_id).scheme == "https"`), any `ValueError` raised by `urlparse` (such as invalid IPv6 formats like `https://]`) was caught but silently ignored with `pass`. This could cause `client_is_https` to remain unassigned or in an unexpected state, leading to subsequent application crashes or unexpected behavior (DoS vector).
**Learning:** Even permissive functions like `urllib.parse.urlparse` can raise `ValueError` on malformed inputs to prevent attacks like CRLF injection. Suppressing these errors leaves the application vulnerable to crashes or logical errors further down the flow.
**Prevention:** Always handle `ValueError` from `urlparse` securely by explicitly rejecting the malformed request (e.g., returning a 400 Bad Request error response) rather than ignoring it or propagating the error.
## 2025-02-21 - Add Security Headers to Consent Page
**Vulnerability:** The OAuth consent page generated via `_consent_page` lacked `Content-Security-Policy` (CSP) and `X-Content-Type-Options` headers, which could increase the risk of MIME-type sniffing, cross-site scripting (XSS), or clickjacking attacks despite the presence of `X-Frame-Options`.
**Learning:** Returning user-facing HTML responses in Python frameworks (like Starlette) requires explicitly setting a full suite of modern security headers, as defaults are typically minimal.
**Prevention:** When returning HTML responses, apply strict security headers including `X-Content-Type-Options: nosniff` and a restrictive `Content-Security-Policy` (e.g., `default-src 'none'; style-src 'unsafe-inline'; form-action 'self'; frame-ancestors 'none';`) to mitigate XSS, MIME-sniffing, and clickjacking vulnerabilities.

## 2024-08-04 - [DoS via UnicodeEncodeError in URL Encoding]
**Vulnerability:** A Denial of Service (DoS) vulnerability existed due to unhandled `UnicodeEncodeError` exceptions. When an attacker provided malformed string input containing lone surrogate characters (e.g., `\ud800`) to endpoints that eventually call `urllib.parse.urlencode` (like `_build_redirect` constructing redirect URIs with query parameters), the unhandled exception would crash the request handling, resulting in a 500 Internal Server Error.
**Learning:** Python's `urlencode` function implicitly calls `.encode("utf-8")` (by default) on parameters. User input containing surrogate characters cannot be encoded in UTF-8 and will raise a `UnicodeEncodeError`. Input validation and error handling for standard library functions must account for these string encoding edge cases to prevent application crashes.
**Prevention:** Always wrap calls to string encoding functions (or functions like `urlencode` that perform encoding internally) with a `try...except UnicodeEncodeError` block when processing user-supplied input, and handle the error gracefully (e.g., by returning a 400 Bad Request or raising a controlled `ValueError` that is caught and translated to a safe HTTP response).
## 2024-05-24 - [Fix manual consent form missing response_type parameter]
**Vulnerability:** A logic error in `_consent_page` failed to include `response_type` in the hidden form inputs. When the form was POSTed, it resulted in a `400 Bad Request` instead of authorizing the client, forcing operators to bypass consent (`auto_approve=True`).
**Learning:** Overlooking required parameters in form construction can inadvertently degrade security posture by compelling users/operators to disable critical security controls (like explicit user consent) in order to make the system functional.
**Prevention:** Ensure integration tests strictly mimic real-world browser submissions (e.g. by parsing HTML forms and POSTing exactly the included inputs) rather than manually crafting POST data that includes fields not actually rendered.
## 2025-02-21 - [DoS via InvalidURL in urlopen]
**Vulnerability:** A Denial of Service (DoS) vulnerability existed when fetching external client metadata documents (`CIMD`). When making network requests with `urllib.request` using a user-supplied URL, it failed to explicitly catch `http.client.InvalidURL` (and `ValueError`). If a maliciously crafted URL contained control characters (like `\r`), `urllib.parse.urlparse` might parse it without error, but the underlying `http.client` crashed with `InvalidURL`, raising an unhandled exception that crashed the server.
**Learning:** Functions in `urllib.request` (like `opener.open()`) can raise non-network exceptions such as `http.client.InvalidURL` or `ValueError` when processing syntactically invalid URLs, even if they bypassed earlier basic `urlparse` checks.
**Prevention:** Always explicitly catch `http.client.InvalidURL` and `ValueError` along with standard network exceptions (`URLError`, `OSError`) when passing arbitrary, user-controlled URLs to `urllib.request` routines.
## 2025-02-15 - Enforce explicit Path=/ for __Host- cookies
**Vulnerability:** The CSRF cookie was using the `__Host-` prefix but relied on the framework's default behavior for the `Path` attribute.
**Learning:** `__Host-` prefixed cookies must explicitly specify `Path=/`. While frameworks like Starlette may default to this, omitting it creates a fragile security boundary that could break and lead to cookie rejection by browsers if defaults change or the code is ported.
**Prevention:** Always explicitly set `path="/"` when configuring `__Host-` cookies to guarantee compliance with RFC 6265bis.
## 2025-02-21 - Fix DoS via UnicodeEncodeError in SQLite Storage Hashing
**Vulnerability:** A Denial of Service (DoS) vulnerability existed in `origo/sqlite_storage.py` where the `_hash` function called `.encode("utf-8")` without catching `UnicodeEncodeError`. If an attacker provided a token or secret containing lone surrogate characters (e.g., `\ud800`), the application would crash with a 500 error when attempting to hash it for a database lookup or storage.
**Learning:** Functions that hash inputs using `hashlib` must first encode strings to bytes. If user input contains surrogate characters, `.encode("utf-8")` will raise a `UnicodeEncodeError`.
**Prevention:** Wrap the encoding and hashing in a `try...except UnicodeEncodeError` block. When handling lookup fields (like tokens or secrets), returning an empty string `""` on error safely guarantees the database query will find no match without crashing the server.
## 2025-02-21 - Fix HTTP Parameter Pollution in Endpoints
**Vulnerability:** A High severity HTTP Parameter Pollution vulnerability existed in `origo/endpoints.py`. In both the `/authorize` and `/token` endpoints, the request parameters (either query string via `request.query_params` or POST body via `request.form()`) were cast directly to a Python `dict()`. This behavior silently drops all but the last parameter value for duplicate keys. This allows attackers to bypass security validations if intermediate proxies or WAFs process the first duplicate parameter while the application processes the latter.
**Learning:** Casting Starlette's `QueryParams` or `FormData` directly to `dict()` silently suppresses duplicate keys. When processing security-critical fields (like `client_id`, `redirect_uri`, `code`, etc.), ignoring duplicates opens up HTTP Parameter Pollution (HPP) vulnerabilities, where a client supplies multiple values and different layers of the stack interpret the request differently.
**Prevention:** Always validate that `len(params.multi_items()) == len(params.keys())` before casting to a dictionary when parsing query or form parameters that should uniquely represent fields. If duplicates exist, explicitly reject the request with a 400 Bad Request error to enforce strict standard compliance (such as RFC 6749).
